# GPU 结构

## 1. 执行层级

- `grid → n 个 block → m 个 thread`
- `block → p 个 warp → 32 个 thread`

## 2. 资源分配单位：block

- 一个 block 绑定到一个 SM，一个 SM 可以绑定多个 block；
- block 需求的寄存器文件大小超过 SM 物理上限时报错；未超过但剩余数量不够分配时，先暂存该 block。

## 3. 最小调度单位：warp

- **SIMT**：同一个 warp 的线程执行相同的指令，大多情况下操作不同的数据；
- 同一个 block 的每个 warp 需求的寄存器文件大小相同，以避免因寄存器文件被物理分成不同区域导致的碎片化——总体够用，但一个分区剩余的不够一个 warp 使用。

## 4. SM（流式多处理器）

- 某个 SM 架构有 4 个 Warp Scheduler；
- 每个 SM 物理划分出 4 个子区域，分别由 1 个 Warp Scheduler 调度。

## 5. Warp Scheduler 调度流程

### 阶段 0：补给与预解码（后台默默运行）

- **地点**：I-Cache → I-Buffer（每个 warp 都有 I-Cache 和 I-Buffer）
- **动作**：当某个 Warp 的 I-Buffer 出现空位时，取指单元从共享的 I-Cache 中一次性取出 128 Bytes（8 条指令）。在填入 I-Buffer 的**同时**，硬件执行“预解码”：利用指令格式中寄存器地址位固定的特性，**快速抠出源寄存器和目标寄存器的编号**，作为附加标签（Tag）随原始指令一同存入该 Warp 的私有 I-Buffer。
- **目的**：将慢速的 I-Cache 访问与快速的指令发射解耦，并在空闲带宽下提前备好“依赖检查”所需的关键信息。

### 阶段 1：仲裁与记分牌检查（每个时钟周期的心脏跳动的开始）

- **地点**：仲裁器 + 记分牌（只属于每个 Warp Scheduler）
- **动作**：仲裁器扫描其管理的所有 Warp。对于每个 Warp，**不读取完整指令**，而是直接读取 I-Buffer 中**预解码好的寄存器编号标签**，并立即查询记分牌（Scoreboard）中的 1-bit 标志位。
  - **源寄存器**必须全为 0（数据就绪）；
  - **目标寄存器**必须为 0（无写后写冲突）。
- **决策**：只要有一个寄存器标志位为 1，该 Warp 就被标记为“未就绪”，本轮淘汰。仲裁器从所有“就绪”的 Warp 中，依据策略（如 GTO/LRR）选出**唯一一个**胜出者。

### 阶段 2：完整译码与操作数收集（发射前的最后一公里）

- **地点**：译码器 + 发射单元（Dispatch Unit）
- **动作**：被选中的指令从 I-Buffer 流出，进入**完整译码器**，拆解出 Opcode、立即数、寻址模式等复杂控制信号。与此同时，发射单元根据译码出的寄存器地址，去**寄存器文件（Register File）**中物理读取源操作数的实际数值。
- **关键动作（锁死）**：在这条指令被确认发射的**同一时刻**，记分牌将指令的**目标寄存器**对应标志位**置为 1**。这意味着该寄存器的旧数据逻辑上作废，后续任何指令想读它都必须等待。

### 阶段 3：执行（发起请求 vs 纯粹计算）

- **地点**：执行单元（FP32/INT/Load-Store 等）
- **动作**：指令（连同操作数）被派发到执行单元的流水线寄存器。
  - **若为计算指令（如 ALU）**：数值直接流入 ALU，几个时钟周期后算出结果。
  - **若为访存指令（如全局内存加载 LDG）**：执行单元算出内存地址后，**立刻将读请求扔进内存总线**，随即结束指令执行。注意，此时**数据绝对没有回来**，记分牌依然死死锁着目标寄存器。

### 阶段 4：写回与唤醒（解除阻塞）

- **地点**：执行单元 → 记分牌
- **动作**：
  - **计算指令**：ALU 算完后，结果数据写回寄存器文件，同时发送“写回完成”信号给记分牌。
  - **访存指令**：等待几百个时钟周期后，数据从显存返回到 SM，写回寄存器文件，同时发送“写回完成”信号给记分牌。
- **最终动作**：记分牌接收到信号，**立即**将对应目标寄存器的标志位**清为 0**。此时，所有等待这个寄存器的后续指令，在下一个周期查询记分牌时，会发现阻碍消失，Warp 重新进入“就绪”池。

## 6. 设计哲学

整个 Warp Scheduler 的设计哲学是：**用极度敏感且廉价的 1-bit 门卫（记分牌），来掩护昂贵且迟钝的执行单元（ALU/内存），使其永不停歇**。只要记分牌不停顿，ALU 就永远有活干。

---

# 内存层级

## 1. L1 Cache 与共享内存（SMEM）

### 1.1 物理本质：256KB 的统一“高速暂存器”

- **位置**：位于 SM（流式多处理器）内部，与 CUDA Core 仅隔几个时钟周期。
- **速度**：延迟约 **30 个时钟周期**，远快于访问全局显存（HBM，数百周期）。
- **构成**：物理上由 **32 个 Bank（存储体）** 组成，每个 Bank 位宽 **32 位（4 字节）**。
- **总带宽**：32 个 Bank 满负荷并行时，一个周期可吞吐 **32 × 4 = 128 字节**。

### 1.2 逻辑划分：两种身份，一块地基（可配置）

这块 256KB SRAM 在运行时可以通过配置静态划分（如 128KB L1 + 128KB SMEM）：

| 功能模块 | 管理者 | 核心职责 | 数据来源 |
| :--- | :--- | :--- | :--- |
| **L1 数据缓存** | **硬件**自动管理 | 缓存来自**全局显存**的数据，对程序员透明。 | 从显存（DRAM）拉取的 128 字节 Cache Line。 |
| **共享内存（SMEM）** | **程序员**显式管理（`__shared__`） | 同一 **Block（线程块）** 内线程间的数据共享与复用。 | 程序员通过代码主动存入的数据。 |

### 1.3 微观编址规则：RAID 0 式的条带化（交错映射）

无论是 L1 硬件填入，还是程序员写入共享内存，数据存入这 32 个 Bank 的规则**完全一致**，类似于磁盘阵列的 RAID 0：

- **映射公式**：**Bank ID = (字节地址 ÷ 4) % 32**
- **物理含义**：
  - 地址 **0~3** 字节 → **Bank 0**
  - 地址 **4~7** 字节 → **Bank 1**
  - ...
  - 地址 **124~127** 字节 → **Bank 31**
  - 地址 **128~131** 字节 → 又回到 **Bank 0**（第二轮循环）
- **周期长度**：32 个 Bank 转一圈正好是 **32 × 4 = 128 字节**，这并非巧合，而是对应了 L1 缓存行（Cache Line）和全局内存合并访问的粒度。

### 1.4 两大核心性能杀手

| 性能陷阱 | 发生阶段 | 根本原因 | 后果 |
| :--- | :--- | :--- | :--- |
| **合并访存失败**<br>（Global Load 效率低） | 步骤 1（地址归类） | 32 个线程访问的地址**过于离散**，横跨几十个 128 字节段。 | 触发大量显存事务，浪费 HBM 带宽。 |
| **Bank Conflict**<br>（SMEM 或 L1 读取慢） | 步骤 3（从 Bank 取数） | 多个线程访问地址差值为 **128 字节的整数倍**（如步长 32 的 int 数组），导致全挤进同一个 Bank。 | Bank 内串行响应，实际带宽降为 1/N。 |

### 1.5 终极分工：硬件 vs 程序员

| 工作内容 | 谁来做？ | 程序员应该怎么做？ |
| :--- | :--- | :--- |
| **Cache Line 搬运** | **硬件**自动 | 无需关心，但需配合步骤 1 写出连续地址。 |
| **Tag 查找与命中/缺失判断** | **硬件**自动（多端口并行） | 无法干预，完全透明。 |
| **全局内存合并访存** | **硬件**基于地址判定 | 代码中确保 Warp 内线程访问**连续地址**（如 `base + tid`）。 |
| **共享内存数据摆放** | **程序员**手动写入 | 明确自己要存什么数据到 `__shared__` 数组。 |
| **Bank Conflict 规避** | **程序员**必须精心设计 | 使用 **Padding（填充）** 技巧（如数组声明为 `[32][33]` 而非 `[32][32]`），破坏步长为 32 的倍数关系。 |
| **显存延迟隐藏** | **硬件**（Warp 调度器）+ **程序员**（Occupancy） | 提高 Block 数量，保证 SM 中有足够多的 Warp 可供切换，掩盖访问延迟。 |

## 2. GPU L1/共享内存完整数据读取流水线（端到端流程）

### 阶段 0：指令发射与地址生成（Warp 级）

- **动作**：SM 中的 Warp 调度器选定一个 Warp（32 个线程），发射一条全局内存加载指令（LD）。

### 阶段 1：地址归类与“合并访存”（LD/ST 单元）

- **动作**：LD/ST 单元的硬件电路对 32 个地址进行**实时分类**。
- **规则**：检查这 32 个地址落在几个 **128 字节对齐的内存段（Cache Line / 段）** 内。
- **结果**：将 32 个独立的地址请求，**压缩合并为 N 个“段查询请求”**（N = 覆盖的段数量，例如地址横跨 0~255 则 N=2）。
- **核心收益**：这一步把对下游 L1 控制器的访问次数从 32 次骤降至 N 次，**极大节省了 Tag 查询端口的带宽**。
- **关键认知**：合并与否只看“地址连续性”，此时尚未涉及 Bank。

### 阶段 2：L1 Tag 并行查找（L1 控制器）

- **动作**：硬件拿着阶段 1 生成的 N 个“段查询请求”（如段 A、段 B），**同时（并行）** 发送给 L1 缓存的 **Tag 阵列（标签阵列）**。
- **物理基础**：Tag 阵列是**多端口（Multi-Ported）**设计，支持同一时钟周期查多个段。
- **结果**：在**1 个（或极少数）时钟周期**内，硬件瞬间得出每个段的命中状态：
  - **状态① 命中（Hit）**：该 128 字节段目前正躺在 L1 缓存的物理 Bank 里。
  - **状态② 缺失（Miss）**：该 128 字节段不在 L1 中，必须去显存（HBM）搬运。

### 阶段 3：分头行动（数据读取 vs 显存填充）

#### 分支 A：对于“命中”的段（走片上路径）

- **动作**：硬件去 L1 数据阵列（即 32 个物理 Bank）中取出对应的 128 字节数据。
- **物理落位规则（核心）**：数据在 Bank 中的分布严格遵守 **RAID 0（条带化）** 规则：
  - `Bank ID = (字节地址 ÷ 4) % 32`
  - 即每个 Bank 存 4 字节，连续 128 字节依次轮转存入 Bank 0 ~ Bank 31。
- **并行瓶颈（Bank Conflict）**：此环节能否“并行”取数，**取决于地址是否争抢同一个 Bank**：
  - **无冲突**：访问的地址分散在不同 Bank 编号上 → **并行**，1~2 个周期取完。
  - **有冲突**：多个地址落在同一个 Bank 的**不同楼层**（如步长 32 的整数倍） → **串行排队**，延迟成倍增加。
- **额外物理限制**：即便无 Bank 冲突，数据返回总线（Data Bus）的物理位宽（如 64 字节/周期）也可能导致数据分 2 个周期流出。

#### 分支 B：对于“缺失”的段（走显存路径）

- **动作**：硬件触发一次对**全局显存（HBM）**的 128 字节连续读取事务。
- **填充过程**：几百个时钟周期后，数据从显存抵达 L1 控制器，硬件将整条 Cache Line**按 4 字节分段，轮询写入 32 个 Bank**（同样遵循 `地址/4 % 32` 的 RAID 0 规则），将数据存入对应楼层。

### 阶段 4：同步等待与数据汇合（Warp 指令原子性）

- **动作**：LD/ST 单元等待阶段 3 中**所有**涉及的段（无论命中还是缺失）全部就绪。
- **关键机制（同步返回）**：在 GPU SIMT（单指令多线程）模型中，**一条加载指令必须等 Warp 内 32 个线程的数据全部到位，才能标记为“完成”**。因此：
  - 若全部命中 → 瞬间汇合，直接写回寄存器。
  - 若部分命中/部分缺失 → **已命中的段数据被暂存在“加载缓冲区（Load Buffer）”**，该 Warp 被标记为 **Stalled（阻塞）**，强制等待缺失段从显存返回。

### 阶段 5：延迟隐藏（Warp 上下文切换）

- **动作**：当 Warp 进入 Stalled 状态后，SM 内的 **Warp 调度器** 不会让计算单元（CUDA Core）空转。
- **操作**：调度器**立即切出**该阻塞 Warp，换入另一个**数据已就绪的 Warp** 执行其算术指令。
- **唤醒**：等缺失段的 128 字节从显存返回并填入 L1 Bank 后，硬件将“加载缓冲区”内的旧数据与新数据拼接，统一写回寄存器。此时该 Warp 被重新标记为 Ready（就绪），排入后续执行队列。

## 3. 贯穿全流程的核心原则表

| 流程阶段 | 判断依据（看什么） | 并行能力 | 编程优化启示 |
| :--- | :--- | :--- | :--- |
| **1. 地址合并** | 地址是否落在**同一个 128 字节对齐段**内 | 只看连续性 | 让 Warp 内线程访问连续地址（如 `base + tid`） |
| **2. Tag 查找** | 段地址是否在 Tag 阵列中 | **绝对并行**（多端口硬件） | 无需操心，硬件自动完成 |
| **3. Bank 取数** | 地址计算出的 `Bank ID` 是否重复 | **有条件并行**（避不开就串行） | 代码中避免步长为 32 倍数的访问模式（Padding） |
| **4. 同步返回** | 是否所有段都就绪（Hit + Miss） | **强制同步**（Warp 锁步） | 依赖其他 Warp 切换来隐藏延迟（提高 Occupancy） |

---

# GPU 上 GEMM 的完整执行流程

## 第一阶段：软件定义（主机端 CPU）

### 1. 任务划分（逻辑分块）

- 开发者定义 Tile 大小（如 `128x128`）。
- 根据输出矩阵 `C` 的维度（`M x N`），计算 Grid 大小：`grid_x = ceil(N/128)`，`grid_y = ceil(M/128)`。
- 每个 Block 被静态分配一个唯一的 `blockIdx`，对应负责输出矩阵 `C` 中的一个 `128x128` Tile。
- **注意**：Tile 划分的是**计算任务**（C 矩阵的输出范围），A 和 B 的划分是动态的，由 K 维度循环中的加载逻辑决定。

### 2. 资源静态预留（核函数启动前）

- **共享内存**：每个 Block 预先分配两块相同大小的共享内存区域（Ping/Pong Buffer），大小通常为 `2 * (BM * BK + BK * BN)`。
- **寄存器文件**：为 Block 内的每个线程预留固定数量的寄存器（如 128~255 个），用于存放 `mma.sync` 指令所需的矩阵碎片（Fragment）。

---

## 第二阶段：Block 内部的 Warp 布局（软件定义）

### 1. Warp 任务划分

- Block 包含的线程数（如 128 个 = 4 Warps）被排列成一个 `2x2` 的 Warp 网格。
- 每个 Warp 负责计算 `128x128` Tile 中的一个 `64x64` 子块。
  - Warp(0,0) → 左上 `64x64`
  - Warp(0,1) → 右上 `64x64`
  - Warp(1,0) → 左下 `64x64`
  - Warp(1,1) → 右下 `64x64`

### 2. K 维度的软件流水线设计

- 开发者定义 `K_tile`（如 64），即每次搬运到共享内存的 K 维度深度。
- 外层 `for` 循环遍历全局 K 维度，步长 = `K_tile`，由程序员**手动编写**。
- 循环内实现**双缓冲（Ping-Pong）** 切换，由循环索引 `k_step` 的奇偶性决定当前批次加载到哪个缓冲区，下一轮计算使用另一个缓冲区。

---

## 第三阶段：数据加载与计算的并行流水线（软硬件协同）

### 1. 异步数据加载（软件发起，DMA 硬件执行）

- 每个 `K_tile` 迭代开始时，代码发起 `cp.async` 指令，从全局内存（HBM）加载当前批次的 A（`128x64`）和 B（`64x128`）数据到共享内存缓冲区（Ping 或 Pong）。
- 加载是**异步**的，由 GPU 的 DMA 引擎在后台执行，不阻塞计算单元。

### 2. 计算与加载重叠（硬件自动流水线）

- 当第 N 批数据被异步加载到缓冲区 X 时，Tensor Core 同时在**使用缓冲区 Y** 计算第 N-1 批数据。
- 这种重叠由代码中的 `wait_previous_load_done()` 同步点控制，确保计算开始前上一批数据已就绪。

---

## 第四阶段：Warp 内部的数据准备（线程级操作）

### 1. 共享内存 → 寄存器（`ldmatrix` 指令）

- Warp 内的 32 个线程协同执行 `ldmatrix` 指令。
- 每个线程根据其 `laneid`（0~31）从共享内存中抓取矩阵碎片（Fragment），存入自己的寄存器。
- 每个线程持有的碎片是**非连续**的，按 Tensor Core 硬件规定的固定布局分布。
- 这一步是 **Warp 级同步** 的，需要所有 32 个线程同时完成。

### 2. 累加器初始化（线程级）

- 每个线程将寄存器中的累加器碎片（C Fragment）清零或加载偏置值。
- **注意**：累加器变量（`c_frag`）必须在 `K_tile` 外层循环**外部**声明，这样它的物理寄存器地址在整个 K 维度遍历期间保持不变，才能实现跨批次累加。

---

## 第五阶段：Tensor Core 核心计算（硬件主导 + 软件控制循环）

### 1. `mma.sync` 指令执行

- Warp 内 32 个线程同步执行 `mma.sync` 指令。
- 硬件从所有线程的寄存器中收集 A、B、C 碎片，送入 Tensor Core 运算单元。
- 单次 `mma.sync` 完成的数学操作：`D = A(16x16) × B(16x16) + C(16x16)`。
- 结果 D 的碎片自动写回各线程寄存器中的累加器变量。

### 2. 累加的层级控制

- **批次内（K_tile/16 次）**：**由程序员手动编写内层 `for` 循环**（通常加 `#pragma unroll` 展开），控制当前 `K_tile` 的 64 层数据需要分 4 次执行 `mma.sync`。这个循环**不是硬件自动拆分的**，而是软件显式控制指令发出次数。编译器会将循环展开为多条连续的 `mma.sync` 指令。**加法本身**（`A*B + C` 中的 `+C`）由 Tensor Core 硬件自动完成。

- **跨批次（K/K_tile 次）**：**由外层 `for` 循环控制**，程序员将累加器变量 `c_frag` 声明在循环外部，确保每个批次的 `mma.sync` 结果累加到**同一个寄存器变量**上。跨批次累加是在**执行下一个 `K_tile` 批次的第一条 `mma.sync` 指令时**，由硬件自动完成的（因为硬件只认寄存器里的“旧值”），但“让这个旧值保留下来”的逻辑由软件通过变量作用域控制。

### 3. 累加器的物理存储

- 每次 `mma.sync` 计算出的 256 个结果（`16x16`），被拆散后**分别存放在当前 Warp 的 32 个线程各自的寄存器中**，每个线程持有 8 个结果元素。
- 不同 Warp 的累加器**互不相通**，因为寄存器是线程私有的。
- 跨批次累加的作用范围是**单个 Warp 内部**。

### 4. 空间铺展（64x64 覆盖）

- 为了填满 Warp 负责的 `64x64` C 子块，需要对 `(64/16) × (64/16) = 16` 个空间位置分别执行 `mma.sync`。
- 每个 Warp 最终需要执行 `16（空间位置）× 4（K_tile内深度迭代）× 16（K_tile批次）= 1024` 次 `mma.sync` 指令来完成其负责的 `64x64x1024` 计算。

---

## 第六阶段：结果写回（软硬件协同）

### 1. 寄存器 → 共享内存

- 一个 Warp 的 `64x64` 子块全部算完后，32 个线程通过 `store_matrix_sync` 指令，将各自寄存器中的 C 碎片聚合写回到共享内存的指定区域。

### 2. 共享内存 → 全局内存（HBM）

- Block 内所有 Warp 都完成各自的 `64x64` 子块后，整个 `128x128` Tile 的结果在共享内存中拼装完整。
- 通过合并访存（Coalesced Access），将整个 Tile 写回全局内存中对应的位置。
- 此操作与下一批 Tile 的计算重叠，避免空闲。

---

## 硬件执行模型补充说明（SIMT 与线程束分化）

- **理想状态**：一个 Warp 的 32 个线程执行完全相同的指令流，Warp 调度器锁步分发指令。
- **分化处理**：若遇条件分支，硬件**串行化**执行各分支路径，用活动掩码（Active Mask）禁用非当前路径线程。Volta+ 架构支持独立线程调度，提高细粒度灵活性。
- **性能代价**：分化导致指令吞吐量下降，代码编写时应尽量避免。

---

## 完整执行流程图（概念性）

```
[CPU 主机端]
    │
    ├─ 定义 Tile=128, K_tile=64
    ├─ 计算 Grid/Block 维度
    └─ 启动核函数，静态分配 SM 资源（共享内存 + 寄存器配额）
           │
           ▼
[GPU 设备端 - Block 0..n]
    │
    ├─ 每个 Block 负责一个 128x128 C Tile
    │      │
    │      ├─ 内部 4 个 Warp (2x2 网格)
    │      │      └─ 每个 Warp 负责 64x64 C 子块
    │      │
    │      ├─ 累加器变量 c_frag 声明在 K_tile 循环外部（保持寄存器旧值）
    │      │
    │      └─ 外层循环 (K_tile 批次, 共 16 批)  ← 程序员手动编写
    │             │
    │             ├─ 异步加载当前 K_tile 到 Ping/Pong 缓冲区
    │             ├─ 等待上一批加载完成
    │             ├─ 使用另一缓冲区计算上一批数据
    │             │      │
    │             │      ├─ Warp 内 32 线程执行 ldmatrix
    │             │      │   （共享内存 → 寄存器碎片）
    │             │      │
    │             │      ├─ 内层循环 (K_tile/16 = 4 次)  ← 程序员手动编写
    │             │      │   └─ 32 线程同步执行 mma.sync
    │             │      │       （Tensor Core 完成 16x16x16 乘加）
    │             │      │       （加法 A*B + C 由硬件自动完成）
    │             │      │
    │             │      └─ 重复 16 个空间位置填满 64x64
    │             │
    │             └─ K 维度遍历完成后，累加器存放完整 64x64 结果
    │
    └─ 聚合写回：寄存器 → 共享内存 → L2 → HBM
```

---

## 关键角色分工总结

| 层级 | 职责 | 关键实体 |
| :--- | :--- | :--- |
| **软件/代码** | 定义 Tile/K_tile、编写外层和内层循环、管理 Ping-Pong、控制累加器生命周期（变量声明位置） | 开发者、CUDA 代码 |
| **指令控制** | 决定 `mma.sync` 的执行次数和顺序（通过循环展开） | 程序员手写 + 编译器优化 |
| **硬件调度** | 将 Block 分配到 SM、Warp 调度、SIMT 锁步执行 | GPU 调度器、Warp Scheduler |
| **数据搬运** | 执行异步加载/存储指令 | DMA 引擎、内存控制器 |
| **数据准备** | 共享内存 → 寄存器碎片（重排布局） | `ldmatrix` 指令、线程寄存器文件 |
| **核心计算** | 执行 16x16x16 矩阵乘加，**自动完成加法（乘加融合）** | Tensor Core 硬件、`mma.sync` 指令 |
| **累加保留** | 跨批次累加依赖寄存器中旧值的保留 | 软件（循环外声明变量）+ 硬件（寄存器持久化） |
| **流水线控制** | 实现计算与加载重叠、隐藏访存延迟 | 软件双缓冲逻辑 + 硬件并发执行 |

---

## Blackwell 架构变化

- **传统 `mma.sync`**：

  ```text
  线程寄存器：A/B/C fragment
          ↓
       mma.sync
          ↓
  线程寄存器：D fragment
  ```

- **Blackwell TCGen05**：

  ```text
  SMEM：A/B
      ↓
  Tensor Core：MMA
      ↓
  TMEM：Accumulator D
      ↓
  下一次 MMA 继续累加
      ↓
  最终 TMEM → 寄存器 → Epilogue
  ```

- **Blackwell 的关键变化**：A/B 主要由 SMEM 提供，累加器 D 放在专用 TMEM 中，而不是长期占用普通线程寄存器。

---

# Attention

## 普通 Attention 中，中间结果（S = Q·Kᵀ、P = softmax(S)）为什么要保存到 HBM？

主要有两个原因：

### 1. 中间结果需要跨 kernel 传递
- `S = Q·Kᵀ`、`P = softmax(S)`、`O = P·V` 这三个运算通常由**三个独立的 kernel** 依次执行。
- 寄存器（以及共享内存）中的数据仅在当前 kernel 运行期间有效。一旦 kernel 结束或切换，其内容就会被回收或覆盖，无法跨 kernel 保留。
- 因此，前一个 kernel 的计算结果**必须先写回 HBM**，再由下一个 kernel 从 HBM 重新读入，才能继续后续计算。

### 2. 寄存器无法跨 block 生命周期存续
- 一个 grid 内的 block 数量往往远多于 SM 能同时驻留的数量。资源不足时，部分 block 只能排队，等待某个 SM 腾出资源后再被调度。
- 每个 block 执行完毕后，它占用的寄存器会被硬件**立即回收**，重新分配给之后调度到该 SM 的新 block。尚未被调度的 block 此时还未产生任何数据。
- 如果 block 在结束前只把结果留在寄存器里，而没有写回 HBM，这些数据就会随着寄存器被回收或覆盖而**永久丢失**。
- 寄存器的生命周期与“正在执行的 block”严格绑定——block 一旦结束，数据即失效。而 block 何时调度、落在哪个 SM 上，由硬件动态决定，kernel 无法控制。

---

## 寄存器溢出（Register Spill）

- **触发条件**：一个 warp 中所有线程需要保存的数据量，超过了分配给它们的寄存器容量。
- **处理方式**：编译器将放不下的数据转存到线程私有的 **local memory** 中。
- **存放位置**：local memory 实际上位于全局内存空间（即 HBM），但访问时会经过 L1/L2 缓存加速。
- **典型场景**：tile 尺寸设置过大，而每个 warp 分到的寄存器数量相对较少，导致部分计算结果或其他中间数据被“挤出”寄存器，被迫存入 local memory。
- **性能代价**：local memory 的访问延迟远高于寄存器，会明显拖慢 kernel 的执行速度。

